// BALL STATS //

// Stop Ratio: Determines velocity reduction when hitting the ground
//             If the number is closer to 0, then it will slow down faster
//             Ex: bowling ball is closer to 0 than basketball

const basketball = {
  color: "orange",
  radius: 0.12,
  stopRatio: -0.92,
};

const tennisball = {
  color: "lime",
  radius: 0.033,
  stopRatio: -0.8,
};

const bowlingball = {
  color: "purple",
  radius: 0.105,
  stopRatio: -0.5,
};
