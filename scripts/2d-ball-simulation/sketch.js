const canvas = document.getElementById("canvas");
const ctx = canvas.getContext("2d");
canvas.width = 400;
canvas.height = 400;

window.onload(() => {
  setup();
  draw();
});

function setup() {}

function draw() {}
